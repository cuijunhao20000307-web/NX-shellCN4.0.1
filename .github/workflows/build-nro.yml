name: Build NX-Shell NRO

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkita64:latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Install available Switch dependencies
        shell: bash
        run: |
          dkp-pacman -Syu --noconfirm || true
          for pkg in \
            switch-dev \
            switch-curl \
            switch-freetype \
            switch-libpng \
            switch-libjpeg-turbo \
            switch-libwebp \
            switch-giflib \
            switch-jansson \
            switch-mesa \
            switch-glad \
            switch-zlib \
            devkitA64; do
            dkp-pacman -S --noconfirm "$pkg" || true
          done

      - name: Restore compatible Dear ImGui source
        run: |
          rm -rf libs/imgui
          git clone --depth 1 --branch v1.88.2 https://github.com/ocornut/imgui.git libs/imgui
          test -f libs/imgui/imgui.cpp
          test -f libs/imgui/misc/freetype/imgui_freetype.cpp

      - name: Verify project files
        run: |
          test -f libs/libnsbmp/libnsbmp.c
          test -f libs/libnsbmp/libnsbmp.h
          test -f libs/lib/libusbhsfs.a
          test -f res/NotoSansCJKsc-Regular.otf

      - name: Build NRO
        run: |
          make clean
          make -j"$(nproc)"
          test -f NX-Shell.nro

      - name: Prepare artifact
        run: |
          mkdir -p release
          cp NX-Shell.nro release/NX-Shell_CN.nro
          cp README.md release/README.md

      - name: Upload NRO artifact
        uses: actions/upload-artifact@v4
        with:
          name: NX-Shell_CN_NRO
          path: release/
          if-no-files-found: error
